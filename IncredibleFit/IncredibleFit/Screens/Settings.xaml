<?xml version="1.0" encoding="utf-8" ?>
<!--Written by Lasse Foster https://github.com/LaFoster00 and Lisa Weickenmeier https://github.com/LisaWckn-->
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:IncredibleFit.ViewModels"
             x:Class="IncredibleFit.Screens.Settings"
             Shell.NavBarIsVisible="False"
             x:DataType="viewModels:SettingsViewModel">

    <VerticalStackLayout Margin="20">
        <Button x:Name="LogOut"
                Text="Log out"
                Command="{Binding LogoutCommand}"/>
        <Button x:Name="DeleteAccount"
                Text="Delete account"
                Command="{Binding DeleteAccountCommand}"/>
        <Button x:Name="VisitIcons8"
                Text="Visit Icons8"
                Command="{Binding VisitIcons8Command}"/>
    </VerticalStackLayout>
</ContentPage>